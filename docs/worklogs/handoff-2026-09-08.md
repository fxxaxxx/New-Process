# Kimi 会话交接（2026-09-08）——换账号后从此处继续

> 用途：更换 Kimi 账号后，让新会话先读本文件，即可恢复全部上下文。
> 新会话开场白建议：「读取 docs/worklogs/handoff-2026-09-08.md，按里面的状态继续」。

## 当前状态
- **全部改动已提交并推送：081592c（main，2026-09-08）**，含数据快照更新（219 表 / 98259 行，gz 18MB）。工作区干净（仅 kimi-export-session_*.md 有意不提交，含明文密码）。
- 最近完成「印喷类加工内容自动写进塑胶采购单明细备注」（frontend-only）：
  - `web/src/pages/plastics/PlasticPurchaseOrderDrawer.tsx`（loadBasis）
  - `web/src/pages/plastics/PlasticPurchaseOrderPage.tsx`（bringFromProduction）
  - 各加一行：`备注: /[喷印]/.test(b.加工内容 ?? "") ? b.加工内容 : undefined`
  - tsc + vitest 336 全过，HMR 已生效。**现在无待办，等用户下一步反馈**（用户会按业务流程继续测试并带截图报问题）。
- 后续「上传」照旧：git add 全部（**排除 kimi-export-session_*.md**）+ 重跑数据快照（见文末）→ commit → push origin main（用户是 owner 可直推）。

## 环境操作要点
- 项目根 `/Users/fovo/Desktop/New-Process`：.NET8 后端（src/ErpApi，localhost:5000）+ React 前端（web/，vite 5173，后台任务长期跑，HMR 自动生效）+ SQL Server（localhost,1433，库 erp / 测试库 erp_test）。
- 后端重启：`kill -9 $(lsof -ti :5000); bash /tmp/start_erp_5000.sh`（输出 ERP-UP 即成；脚本 source /tmp/erp_env_kv.sh）。
  - 若 /tmp 被清需重建：ERP_DB=`Server=localhost,1433;Database=erp;User Id=sa;Password=<见 /tmp/erp_env_kv.sh>;TrustServerCertificate=True`，ERP_JWT_KEY 任意64位hex，Paiji__User / Paiji__Password、SprayPlan__User / SprayPlan__Password 也在该文件。
  - **所有外部系统凭据（SQL Server、排产、喷油、数据库密码）都在本机 `/tmp/erp_env_kv.sh`，严禁写进 git。** 换账号不影响，同一台机器直接读。
- sqlcmd：`~/.local/sqlcmd/sqlcmd -S localhost,1433 -U sa -P '<同上>' -N disable -d erp`，中文脚本必须 `-i /dev/stdin <<'EOF'`；查询中文列名输出乱码正常。
- 登录：POST /api/auth/login {"用户":"admin","密码":"admin123"} → 「令牌」。冒烟用 python3 urllib（中文 query 必须 urlencode）。
- 测试基线：
  - 后端 `export PATH="$HOME/.dotnet:$PATH"; export ERP_TEST_DB='<同连接串,Database=erp_test>'; dotnet test tests/ErpApi.Tests` —— **799 过 / 1 失败**（`PlasticRawMaterialSummaryServiceDbTests.RawMaterialSummary_current_stock_plus_period_scrap` 是既有坏测试：测旧口径"塑胶报废扣原料库存"，实现后来有意改成报废不计入，测试没跟着改；用户知情，未删）。
  - 前端 `cd web && export PATH="$HOME/.local/node/bin:$PATH" && npx tsc -b && npx vitest run` —— 基线 67 文件 336 全过。
- Excel 解析 venv：/tmp/erp_xls_env（pandas/xlrd/openpyxl，被清重建很快）。
- 复杂功能用 coder 子代理；小编辑自己做但 Edit 前必须重读文件。

## 外部系统对接（核心资产，勿重做）
### 排产系统（AI注塑啤机排产）
- http://8.148.146.194/paiji/ ，Basic 认证（凭据在 /tmp/erp_env_kv.sh，严禁进 git）。
- 源码 github.com/charlesskl/RR-Portal，本地 /tmp/RR-Portal（被清就重 clone）。
- 供应商含「兴信A」→ AT 测试车间（上游 PR #619 已加，密码 test1234）；A车间→A、B车间→B、华登→C、其余→W。
- API：`/paiji/api/orders` + `/paiji/api/warehouse-orders`。
- ERP 侧：`src/ErpApi/Integrations/Paiji/`。反向同步 PaijiSyncWorker 每 300 秒拉 AT 已入库单生成 ERP 塑胶入仓单（未审核），防回环靠 `[排产同步记录]`。

### 喷油部排期系统（sprayplan-test）
- http://8.148.146.194/sprayplan-test/ ，两层认证：Basic（同上）+ 应用层 JWT（POST /api/auth/login，用 Set-Cookie 的 sprayplan_session，401 自动重登）。
- **推送规则**：塑胶采购订单审核且供应商名含「喷油部」→ 按款号分组建单（externalOrderNo=单号，多款号=单号-款号；产品不存在先 POST /api/products 建；partQtys=明细行；remark="ERP:塑胶采购订单…"）。
- 新单**必须 draft 状态等人工接单**（用户明确要求）——反审核后重推不走恢复（archived 只能恢复成 received），而是换号新建（-R2/-R3…选第一个空闲号），回收站旧单不管。**喷油部的单不推排产系统**（已跳过）。反审核 DELETE 软删。
- 推送记录复用 `[排产推送记录]`（排产端='sprayplan-test'，车间='喷油部'）。
- ERP 侧：`src/ErpApi/Integrations/SprayPlan/`（SprayPlanOptions/SprayPlanMapper/SprayPlanPushService）。
- **绝不用** DELETE /api/orders/recycle-bin（整体清空接口，曾误调删了 19 张测试单，已向用户坦白）。
- sprayplan 源码在 /tmp/RR-Portal/apps/喷油部/喷油排期系统/（Next.js+ASP.NET+SQLite）。注意 sprayplan-test 是 ECS 上手工跑的第二实例，仓库里只有 /sprayplan。

## 印喷类规则（用户定的统一口径）
加工内容含「喷」或「印」（喷油/移印/印喷/印刷）= 印喷工序，一视同仁。落点：
1. `SecondProcessCategory.cs` 归一（`s.Contains('喷') || s.Contains('印')`）和 `web/src/utils/secondProcess.ts` 同款——移印已加入（此前只认喷）；
2. 塑胶采购单带入过滤（`PlasticPurchaseOrderPage.tsx:52-53`，供应商名含「喷油」时只带 /[喷印]/ 行）；
3. 明细备注自动填加工内容（最近一次改动）。

## 本会话已落地（全部实测过，已随 081592c 提交）
- **成品库存页**：菜单入口（menuTree.tsx g-ship 组）+ 默认成品仓 + useAutoReload；表头 客户/配件编号/产品货号/产品名称/产品装配名称/库存数量（新服务方法 `FinishedGoodsByItemAsync`，按配件编号聚合，出仓侧靠 款号→配件编号 映射 CTE）；点配件编号弹流水详情（`FinishedGoodsLedgerAsync`，GET /api/finished-inventory/ledger）。`FinishedGoodsAsync` 旧方法没动（盘点基准和测试还靠它）。MaterialLineTable 成品仓库存列 key 同步改了。
- **领料单「走货」联动排期**：领料单整单审核或分次出库全部出完时，备注含「走货」→ `SchedulingService.MarkShippedForMaterialIssueAsync`：有生产单号的行按 生产制单.合同号=排期.PO号 + 生产制单货号.货号=排期.货号 精确翻「已走货」；无生产单号按货号兜底。反审核不回退（Excel 重导是权威源）。测试在 SchedulingServiceDbTests（需先种 物料资料 父行，领料明细单.物料编号有 FK）。
- **MasterCrudController.Update 修复**：`entity.ID = id;`（编辑保存误报「编号已存在」的 bug，所有基础资料页受益）。
- **92125 数据补录**（`db/85_92125_加工内容.sql`，幂等已执行 erp 库）：塑胶物料资料 移印×12 + 印喷×6 + NULL×132；塑胶共用物料表 92125 全表 3075 行从「喷油」纠正为 移印×246/印喷×123/NULL×2706；工模表补堵模啤备注 4 行；修外购件/BOM 串名 126 行。遗留：紫猫外购件编号偏移一位（源文件印错，编号自洽未动）。
- **塑胶采购订单明细加「加工内容」列**（`db/86_plastic_po_line_processing.sql`，erp+erp_test 都执行了）：DTO/Create/Update/Get 全链路；抽屉和订单制作页都加了列。
- **部门信息加 20 喷油部**。
- 测试：SprayPlanMappingTests 12 个、SecondProcessCategoryTests 加 6 个移印用例、PlasticPurchaseOrderServiceDbTests 加加工内容断言。
- 排产/喷油凭据在 /tmp/erp_env_kv.sh；appsettings.json 只有空占位。

## 用户业务背景
- 92125 一窝蛋（ZURU）：6 猫（暹罗/紫/橘/黑/棕白/粉白），生产单 SC20260903001（-3L）/SC20260904002/SC20260907002。
- 塑胶流程：啤机加工→白件入塑胶仓（排产同步）→需印喷的领去喷油（白件领料单）→喷油件入仓→装配领料→半成品/成品仓→走货（领料单备注「走货」→排期已走货）。
- 测试单：SP20260907002（喷油部，已审核，sprayplan 侧是 SP20260907002-R2 draft id=29）。

## 悬而未决
- 用户问过「可以做一个手机也可以用的吗」——AskUserQuestion 被 dismiss，**没回答**。他再提就按「整站自适应优先」推进。

## 数据快照（上传时必做）
```bash
export PATH="$HOME/.dotnet:$PATH"
export ERP_DB='<erp 库连接串>'
dotnet run --project tools/DbExport
cd db/snapshot && gzip -kf full_data_snapshot.sql
```
