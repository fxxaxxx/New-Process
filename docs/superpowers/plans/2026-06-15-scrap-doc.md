# 报废单（物料报废）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 照退料单纵切克隆出「报废单」物料单据——录入/审核/复制/打印全套复用现有通用组件，审核过账后从物料库存**扣减**（唯一与退料单不同：库存方向取负）。

**Architecture:** 后端新增 `Features/Materials/MaterialScrap/` 三件套（Controller/Service/Dtos），镜像 `MaterialReturn`，仅改名 退料→报废、TL→BF、material-returns→material-scraps。库存引擎 `MaterialInventoryService.LedgerUnion` 追加一支 `报废明细单 数量*-1`。DB 加 `报废单`+`报废明细单` 两表（含审核留痕列）。过账白名单、权限目录、权限种子、前端一条 config + 菜单路径 各补一处。前端路由是通配 `materials/:doc` → 查 config，故零路由代码改动。

**Tech Stack:** .NET 8 ASP.NET Core, Dapper, SQL Server LocalDB (erp/erp_test, Chinese_PRC_CI_AS), xUnit + Xunit.SkippableFact, React 18 + TS + Vite + Ant Design v6 + Vitest。

---

## 前置约定（所有任务通用）

- 工作目录 `D:\WebpageERP`。Windows 用 PowerShell；`dotnet` 不在 PATH 时刷新：`$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")`。
- DB 集成测试需环境变量（shell 为空时）：`$env:ERP_TEST_DB = [Environment]::GetEnvironmentVariable("ERP_TEST_DB","User")`、`$env:ERP_JWT_KEY = [Environment]::GetEnvironmentVariable("ERP_JWT_KEY","User")`、`$env:ERP_DB = [Environment]::GetEnvironmentVariable("ERP_DB","User")`。
- 跑后端单类测试：`dotnet test --filter "FullyQualifiedName~MaterialScrapServiceDbTests"`；全量 `dotnet test`。前端：`npm --prefix web run test -- --run`、`npm --prefix web run build`。
- **跑后端测试/建库前必须停掉正在运行的 ErpApi**（占 `bin/Debug/ErpApi.exe` 锁，否则 Debug 构建 MSB3027）。
- 本机系统代理 127.0.0.1:7892 会劫持 PowerShell `Invoke-RestMethod` 打本地 API；冒烟用 `HttpClientHandler{UseProxy=false}` 的 .NET HttpClient 或 puppeteer（`tmp/shot`，浏览器不受影响）。
- 提交规范：commit 末尾 `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`。git 报 LF→CRLF 警告正常。
- 分支策略：建特性分支 `feat-scrap-doc` → 全绿后合并 master（与历次一致）。
- **已有可复用件（直接复用，勿重造）**：通用单据组件 `MaterialDocPage`/`MaterialDocCreateDrawer`/`MaterialDocDetailDrawer`/`MaterialLineTable`（含 ProductionPicker/MaterialPicker/EmployeePicker、复制单、打印）；引擎 `IDocumentNumberGenerator`/`IPostingEngine`/`IPermissionService`/`IAuditLogger`/`PeriodLockService`/`IMaterialInventoryService`；测试 `DbFixture`(`[Collection("db")]`、`fx.Open()`、`fx.Available`)、`P3TestData`(`P3TestData.仓库="物料仓"`、`Seed`/`Cleanup`)。

---

## Task 1: 数据库 — 建 报废单 + 报废明细单 两表

**Files:**
- Create: `db/14_scrap_doc.sql`
- Modify: `db/run-db.ps1:19`（在 13 之后追加 14）

镜像 `退料单`/`退料明细单`（`db/01_rebuild_schema.sql:2586-2630`），把列名 退料部门/退料人 改 报废部门/报废人，并直接含审核留痕列 `审核人/审核日期`（避免依赖 03 的清单循环）。幂等 `IF OBJECT_ID ... IS NULL`，可在 erp/erp_test 重复执行。

- [ ] **Step 1: 写迁移脚本 `db/14_scrap_doc.sql`**

```sql
-- 报废单（物料报废）：两层 报废单 + 报废明细单，镜像 退料单/退料明细单。
-- 含审核留痕列 审核人/审核日期（与 03_p0_additions 对其它可过账表所用类型一致）。
-- 幂等：可在 ERP_TEST_DB / ERP_DB 重复执行。
SET XACT_ABORT ON;

IF OBJECT_ID(N'[报废单]', N'U') IS NULL
BEGIN
    CREATE TABLE [报废单] (
        [ID] bigint IDENTITY(1,1) PRIMARY KEY,
        [单号] nvarchar(20) NULL,
        [日期] datetime2(0) NULL,
        [生产单号] nvarchar(30) NULL,
        [报废部门] nvarchar(20) NULL,
        [报废人] nvarchar(10) NULL,
        [仓库] nvarchar(20) NULL,
        [数量] decimal(18,4) NULL,
        [金额] decimal(18,4) NULL,
        [操作员] nvarchar(10) NULL,
        [审核] nvarchar(2) NULL,
        [审核人] nvarchar(20) NULL,
        [审核日期] datetime2(0) NULL,
        [备注] nvarchar(150) NULL,
        [打印次数] int NULL,
        [已阅用户] nvarchar(100) NULL
    );
END

IF OBJECT_ID(N'[报废明细单]', N'U') IS NULL
BEGIN
    CREATE TABLE [报废明细单] (
        [ID] bigint IDENTITY(1,1) PRIMARY KEY,
        [单号] nvarchar(20) NULL,
        [日期] datetime2(0) NULL,
        [生产单号] nvarchar(30) NULL,
        [款号] nvarchar(40) NULL,
        [合同号] nvarchar(30) NULL,
        [客户款号] nvarchar(30) NULL,
        [报废部门] nvarchar(20) NULL,
        [报废人] nvarchar(10) NULL,
        [仓库] nvarchar(20) NULL,
        [物料类别] nvarchar(20) NULL,
        [条码号] nvarchar(20) NULL,
        [物料编号] nvarchar(20) NULL,
        [物料名称] nvarchar(40) NULL,
        [规格] nvarchar(20) NULL,
        [颜色] nvarchar(20) NULL,
        [单位] nvarchar(10) NULL,
        [数量] decimal(18,4) NULL,
        [库存单价] decimal(18,4) NULL,
        [库存金额] decimal(18,4) NULL,
        [单价] decimal(18,4) NULL,
        [金额] decimal(18,4) NULL,
        [备注] nvarchar(150) NULL,
        [预算数量] decimal(18,4) NULL,
        [预算单价] decimal(18,4) NULL,
        [打印次数] int NULL
    );
    CREATE INDEX IX_报废明细单_单号 ON [报废明细单]([单号]);
END
```

- [ ] **Step 2: 把 14 加进建库流水 `db/run-db.ps1`**

把第 19 行 `(Join-Path $dir "13_purchase_return.sql")` 改为以它结尾加一行（注意去掉原行尾、补反引号续行）：

```powershell
  (Join-Path $dir "13_purchase_return.sql") `
  (Join-Path $dir "14_scrap_doc.sql")
```

- [ ] **Step 3: 应用到测试库与开发库**

先确保 ErpApi 未在跑（占锁）。然后：

```powershell
$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")
./db/run-db.ps1 -ConnectionString ([Environment]::GetEnvironmentVariable("ERP_TEST_DB","User"))
./db/run-db.ps1 -ConnectionString ([Environment]::GetEnvironmentVariable("ERP_DB","User"))
```

Expected: 两次都跑完无报错（DbDeploy 逐文件执行，14 幂等建表）。

- [ ] **Step 4: 验证两表存在**

```powershell
dotnet run --project tools/DbDeploy -- ([Environment]::GetEnvironmentVariable("ERP_TEST_DB","User")) `
  "inline:SELECT CASE WHEN OBJECT_ID(N'[报废单]') IS NOT NULL AND OBJECT_ID(N'[报废明细单]') IS NOT NULL THEN 'OK' ELSE 'MISSING' END"
```
> 若 DbDeploy 不支持 `inline:`，改用任一 .NET/SSMS 查询确认 `报废单`、`报废明细单`、以及 `报废单` 含 `审核人`/`审核日期` 列。Expected: 两表存在。

- [ ] **Step 5: Commit**

```bash
git add db/14_scrap_doc.sql db/run-db.ps1
git commit -m "feat(报废单): db 建 报废单+报废明细单 两表(镜像退料单,含审核留痕列)"
```

---

## Task 2: 后端 DTOs

**Files:**
- Create: `src/ErpApi/Features/Materials/MaterialScrap/MaterialScrapDtos.cs`

镜像 `MaterialReturnDtos.cs`，退料→报废。复用共享 `MaterialDocLineDto`（命名空间 `ErpApi.Features.Materials`）。

- [ ] **Step 1: 写 DTOs**

```csharp
using ErpApi.Features.Materials;
namespace ErpApi.Features.Materials.MaterialScrap;

public sealed class MaterialScrapCreateDto
{
    public string? 报废部门 { get; set; }
    public string? 报废人 { get; set; }
    public string? 仓库 { get; set; }
    public string? 备注 { get; set; }
    public List<MaterialDocLineDto> 明细 { get; set; } = [];
}

public sealed class MaterialScrapHeaderDto
{
    public long ID { get; set; }
    public string? 单号 { get; set; }
    public DateTime? 日期 { get; set; }
    public string? 报废部门 { get; set; }
    public string? 报废人 { get; set; }
    public string? 仓库 { get; set; }
    public decimal? 数量 { get; set; }
    public decimal? 金额 { get; set; }
    public string? 操作员 { get; set; }
    public string? 审核 { get; set; }
    public string? 审核人 { get; set; }
    public string? 备注 { get; set; }
}

public sealed class MaterialScrapDetailDto
{
    public MaterialScrapHeaderDto? 单头 { get; set; }
    public List<MaterialDocLineDto> 明细 { get; set; } = [];
}
```

- [ ] **Step 2: 编译确认**

Run: `dotnet build src/ErpApi/ErpApi.csproj`
Expected: 成功（DTO 暂未被引用，编译通过即可）。

- [ ] **Step 3: Commit**

```bash
git add src/ErpApi/Features/Materials/MaterialScrap/MaterialScrapDtos.cs
git commit -m "feat(报废单): 后端 DTOs(Create/Header/Detail)"
```

---

## Task 3: 后端 Service（TDD）

**Files:**
- Create: `src/ErpApi/Features/Materials/MaterialScrap/MaterialScrapService.cs`
- Test: `tests/ErpApi.Tests/MaterialScrapServiceDbTests.cs`

镜像 `MaterialReturnService.cs`：`DocType="报废单"`、`Prefix="BF"`，表名 报废单/报废明细单，列 报废部门/报废人。

- [ ] **Step 1: 写失败测试 `MaterialScrapServiceDbTests.cs`**

```csharp
using Dapper;
using ErpApi.Engines.DocumentNumber;
using ErpApi.Features.Materials;
using ErpApi.Features.Materials.MaterialScrap;
using ErpApi.Infrastructure.Db;
using Microsoft.Extensions.Configuration;
using Xunit;

[Collection("db")]
public class MaterialScrapServiceDbTests(DbFixture fx)
{
    private ISqlConnectionFactory Factory()
    {
        var cfg = new ConfigurationBuilder().AddInMemoryCollection(
            new Dictionary<string, string?> { ["Erp:ConnectionStringEnvVar"] = "ERP_TEST_DB" }).Build();
        return new SqlConnectionFactory(cfg);
    }

    private MaterialScrapService Svc() => new(Factory(), new DocumentNumberGenerator());

    private static MaterialScrapCreateDto Dto() => new()
    {
        报废部门 = "车间一", 报废人 = "李四", 仓库 = P3TestData.仓库,
        明细 = [ new MaterialDocLineDto { 物料编号 = "P3M01", 物料名称 = "P3面料", 规格 = "规格A", 单位 = "米", 数量 = 5, 单价 = 10 } ]
    };

    [SkippableFact]
    public async Task Create_then_Get_then_Delete()
    {
        Skip.IfNot(fx.Available, "未设置 ERP_TEST_DB");
        using var c = fx.Open();
        P3TestData.Seed(c);
        var 单号 = await Svc().CreateAsync(Dto(), "tester");
        try
        {
            Assert.StartsWith("BF", 单号);
            Assert.Equal(5m, c.ExecuteScalar<decimal>("SELECT [数量] FROM [报废单] WHERE [单号]=@单号", new { 单号 }));
            var detail = await Svc().GetAsync(单号);
            Assert.Single(detail!.明细);
            Assert.Equal("车间一", detail.单头!.报废部门);
            Assert.True(await Svc().DeleteAsync(单号));
            Assert.Equal(0, c.ExecuteScalar<int>("SELECT COUNT(*) FROM [报废明细单] WHERE [单号]=@单号", new { 单号 }));
        }
        finally
        {
            c.Execute("DELETE FROM [报废明细单] WHERE [单号]=@单号", new { 单号 });
            c.Execute("DELETE FROM [报废单] WHERE [单号]=@单号", new { 单号 });
            P3TestData.Cleanup(c);
        }
    }

    [SkippableFact]
    public async Task Create_persists_生产单号_款号()
    {
        Skip.IfNot(fx.Available, "未设置 ERP_TEST_DB");
        using var c = fx.Open();
        P3TestData.Seed(c);
        var dto = Dto();
        dto.明细[0].生产单号 = "MO-2026-001";
        dto.明细[0].款号 = "K123";
        var 单号 = await Svc().CreateAsync(dto, "tester");
        try
        {
            var detail = await Svc().GetAsync(单号);
            var line = Assert.Single(detail!.明细);
            Assert.Equal("MO-2026-001", line.生产单号);
            Assert.Equal("K123", line.款号);
        }
        finally
        {
            c.Execute("DELETE FROM [报废明细单] WHERE [单号]=@单号", new { 单号 });
            c.Execute("DELETE FROM [报废单] WHERE [单号]=@单号", new { 单号 });
            P3TestData.Cleanup(c);
        }
    }

    [SkippableFact]
    public async Task Create_rejects_empty_lines()
    {
        Skip.IfNot(fx.Available, "未设置 ERP_TEST_DB");
        var dto = Dto(); dto.明细 = [];
        await Assert.ThrowsAsync<ArgumentException>(() => Svc().CreateAsync(dto, "tester"));
    }

    [SkippableFact]
    public async Task Create_rejects_blank_warehouse()
    {
        Skip.IfNot(fx.Available, "未设置 ERP_TEST_DB");
        var dto = Dto(); dto.仓库 = null;
        await Assert.ThrowsAsync<ArgumentException>(() => Svc().CreateAsync(dto, "tester"));
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `dotnet test --filter "FullyQualifiedName~MaterialScrapServiceDbTests"`
Expected: 编译失败（`MaterialScrapService` 不存在）。

- [ ] **Step 3: 写 Service 实现 `MaterialScrapService.cs`**

```csharp
using Dapper;
using ErpApi.Engines.DocumentNumber;
using ErpApi.Features.MasterData;
using ErpApi.Infrastructure.Db;
namespace ErpApi.Features.Materials.MaterialScrap;

// 报废单（物料报废，库存−）。两层：报废单 + 报废明细单。库存方向由 MaterialInventoryService 统一处理。
public sealed class MaterialScrapService(ISqlConnectionFactory factory, IDocumentNumberGenerator docNo)
{
    public const string DocType = "报废单";
    public const string Prefix = "BF";   // 报废单号 = BF + yyyyMMdd + 3位流水

    public async Task<string> CreateAsync(MaterialScrapCreateDto dto, string user)
    {
        if (dto.明细.Count == 0) throw new ArgumentException("报废单至少要有一行物料明细");
        if (string.IsNullOrWhiteSpace(dto.仓库)) throw new ArgumentException("报废单必须指定仓库");
        var 数量合计 = dto.明细.Sum(l => l.数量);
        var 金额合计 = dto.明细.Sum(l => l.数量 * (l.单价 ?? 0));
        var now = DateTime.Now;

        using var c = factory.Create();
        await c.OpenAsync();
        using var tx = c.BeginTransaction();

        var 单号 = await docNo.NextAsync(DocType, Prefix, now, c, tx);

        await c.ExecuteAsync(@"
INSERT INTO [报废单]([单号],[日期],[报废部门],[报废人],[仓库],[数量],[金额],[操作员],[审核],[备注])
VALUES(@单号,@日期,@报废部门,@报废人,@仓库,@数量,@金额,@操作员,'0',@备注)",
            new { 单号, 日期 = now, dto.报废部门, dto.报废人, dto.仓库,
                  数量 = 数量合计, 金额 = 金额合计, 操作员 = user, dto.备注 }, tx);

        foreach (var l in dto.明细)
            await c.ExecuteAsync(@"
INSERT INTO [报废明细单]([单号],[日期],[仓库],[物料类别],[物料编号],[物料名称],[规格],[颜色],[单位],[数量],[单价],[金额],[备注],[生产单号],[款号])
VALUES(@单号,@日期,@仓库,@物料类别,@物料编号,@物料名称,@规格,@颜色,@单位,@数量,@单价,@金额,@备注,@生产单号,@款号)",
                new { 单号, 日期 = now, dto.仓库, l.物料类别, l.物料编号, l.物料名称, l.规格, l.颜色, l.单位,
                      l.数量, 单价 = l.单价 ?? 0, 金额 = l.数量 * (l.单价 ?? 0), l.备注, l.生产单号, l.款号 }, tx);

        tx.Commit();
        return 单号;
    }

    public async Task<PagedResult<MaterialScrapHeaderDto>> ListAsync(int page, int size, string? keyword)
    {
        if (page < 1) page = 1;
        if (size < 1 || size > 200) size = 20;
        var kw = string.IsNullOrWhiteSpace(keyword) ? null : $"%{keyword.Trim()}%";
        using var c = factory.Create();
        using var multi = await c.QueryMultipleAsync(@"
SELECT COUNT(*) FROM [报废单]
WHERE @kw IS NULL OR [单号] LIKE @kw OR [报废部门] LIKE @kw OR [报废人] LIKE @kw OR [备注] LIKE @kw;
SELECT [ID],[单号],[日期],[报废部门],[报废人],[仓库],[数量],[金额],[操作员],[审核],[审核人],[备注]
FROM [报废单]
WHERE @kw IS NULL OR [单号] LIKE @kw OR [报废部门] LIKE @kw OR [报废人] LIKE @kw OR [备注] LIKE @kw
ORDER BY [ID] DESC OFFSET (@page-1)*@size ROWS FETCH NEXT @size ROWS ONLY;",
            new { kw, page, size });
        var total = await multi.ReadFirstAsync<int>();
        var items = (await multi.ReadAsync<MaterialScrapHeaderDto>()).AsList();
        return new PagedResult<MaterialScrapHeaderDto>(items, total);
    }

    public async Task<MaterialScrapDetailDto?> GetAsync(string 单号)
    {
        using var c = factory.Create();
        using var multi = await c.QueryMultipleAsync(@"
SELECT [ID],[单号],[日期],[报废部门],[报废人],[仓库],[数量],[金额],[操作员],[审核],[审核人],[备注]
FROM [报废单] WHERE [单号]=@单号;
SELECT [ID],[物料编号],[物料名称],[物料类别],[规格],[颜色],[单位],[数量],[单价],[金额],[备注],[生产单号],[款号]
FROM [报废明细单] WHERE [单号]=@单号 ORDER BY [ID];",
            new { 单号 });
        var header = await multi.ReadFirstOrDefaultAsync<MaterialScrapHeaderDto>();
        if (header is null) return null;
        var lines = (await multi.ReadAsync<MaterialDocLineDto>()).AsList();
        return new MaterialScrapDetailDto { 单头 = header, 明细 = lines };
    }

    public async Task<bool> DeleteAsync(string 单号)
    {
        using var c = factory.Create();
        await c.OpenAsync();
        using var tx = c.BeginTransaction();
        var 审核 = await c.ExecuteScalarAsync<string?>(
            "SELECT ISNULL([审核],'0') FROM [报废单] WHERE [单号]=@单号", new { 单号 }, tx);
        if (审核 is null) return false;
        if (审核 == "1") throw new InvalidOperationException("已审核的报废单不能删除，请先反审核。");
        await c.ExecuteAsync("DELETE FROM [报废明细单] WHERE [单号]=@单号", new { 单号 }, tx);
        await c.ExecuteAsync("DELETE FROM [报废单] WHERE [单号]=@单号", new { 单号 }, tx);
        tx.Commit();
        return true;
    }
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `dotnet test --filter "FullyQualifiedName~MaterialScrapServiceDbTests"`
Expected: 4 passed（设了 ERP_TEST_DB 时跑真库；否则 Skipped）。

- [ ] **Step 5: Commit**

```bash
git add src/ErpApi/Features/Materials/MaterialScrap/MaterialScrapService.cs tests/ErpApi.Tests/MaterialScrapServiceDbTests.cs
git commit -m "feat(报废单): 后端 Service(建单/列表/详情/删除)+往返测试"
```

---

## Task 4: 过账白名单登记

**Files:**
- Modify: `src/ErpApi/Engines/Posting/PostableDocuments.cs:18`

在 Map 里 退料单 同行附近加 报废单（单号列=单号）。

- [ ] **Step 1: 改白名单**

把这一行：
```csharp
            ["领料单"] = "单号", ["退料单"] = "单号", ["调拨单"] = "单号", ["盘点单"] = "单号",
```
改为：
```csharp
            ["领料单"] = "单号", ["退料单"] = "单号", ["报废单"] = "单号", ["调拨单"] = "单号", ["盘点单"] = "单号",
```

- [ ] **Step 2: 编译确认**

Run: `dotnet build src/ErpApi/ErpApi.csproj`
Expected: 成功。

- [ ] **Step 3: Commit**

```bash
git add src/ErpApi/Engines/Posting/PostableDocuments.cs
git commit -m "feat(报废单): 纳入过账白名单(报废单→单号)"
```

---

## Task 5: 库存引擎追加报废分支（TDD）

**Files:**
- Modify: `src/ErpApi/Engines/Inventory/MaterialInventoryService.cs:6,22`
- Test: `tests/ErpApi.Tests/MaterialInventoryDbTests.cs`（新增一个测试方法）

报废明细单 `数量*-1` 计入（只认审核='1'）。

- [ ] **Step 1: 在库存测试里加失败测试**

在 `tests/ErpApi.Tests/MaterialInventoryDbTests.cs` 类内追加（放在最后一个 `}` 之前）：

```csharp
    [SkippableFact]
    public async Task StockOf_subtracts_approved_报废()
    {
        Skip.IfNot(fx.Available, "未设置 ERP_TEST_DB");
        using var c = fx.Open();
        Cleanup(c);
        c.Execute("DELETE FROM [报废明细单] WHERE [物料编号]=N'P3M01'");
        c.Execute("DELETE FROM [报废单] WHERE [单号] IN (N'P3BF01')");
        c.Execute("INSERT INTO [物料资料]([物料编号],[物料名称],[单位]) VALUES(N'P3M01',N'P3面料',N'米')");
        c.Execute("INSERT INTO [采购入仓单]([单号],[仓库],[审核]) VALUES(N'P3RK01',N'物料仓','1')");
        c.Execute(@"INSERT INTO [采购入仓明细单]([单号],[仓库],[物料编号],[物料名称],[单位],[数量])
                    VALUES(N'P3RK01',N'物料仓',N'P3M01',N'P3面料',N'米',100)");
        c.Execute("INSERT INTO [报废单]([单号],[仓库],[审核]) VALUES(N'P3BF01',N'物料仓','1')");
        c.Execute(@"INSERT INTO [报废明细单]([单号],[仓库],[物料编号],[物料名称],[单位],[数量])
                    VALUES(N'P3BF01',N'物料仓',N'P3M01',N'P3面料',N'米',20)");
        var stock = await Svc().StockOfAsync("P3M01", null);
        Assert.Equal(80m, stock);   // 入100 − 报废20 = 80
        c.Execute("DELETE FROM [报废明细单] WHERE [物料编号]=N'P3M01'");
        c.Execute("DELETE FROM [报废单] WHERE [单号] IN (N'P3BF01')");
        Cleanup(c);
    }
```

- [ ] **Step 2: 跑测试确认失败**

Run: `dotnet test --filter "FullyQualifiedName~MaterialInventoryDbTests.StockOf_subtracts_approved_报废"`
Expected: FAIL（断言 80 ≠ 100，因为引擎还没算报废）。

- [ ] **Step 3: 给 LedgerUnion 追加报废分支**

`MaterialInventoryService.cs:22` 把 `LedgerUnion` 字符串末尾（`采购退仓明细单` 那段之后、结束的 `";` 之前）追加：

```csharp
UNION ALL
SELECT d.[物料编号],d.[物料名称],d.[规格],d.[单位],d.[仓库], d.[数量]*-1
    FROM [报废明细单] d JOIN [报废单] h ON h.[单号]=d.[单号] WHERE ISNULL(h.[审核],'0')='1'";
```
（即把原来 `采购退仓明细单` 行末的 `";` 去掉换成 `UNION ALL`，新分支结尾补 `";`。）

同时把第 6 行注释更新为：
```csharp
// 算法1（物料口径）：物料库存 = 采购入仓(+) + 退料(+) − 领料(−) − 采购退仓(−) − 报废(−)，仅审核='1'，按 物料编号×仓库 汇总。
```

- [ ] **Step 4: 跑测试确认通过（含原有库存测试不回归）**

Run: `dotnet test --filter "FullyQualifiedName~MaterialInventoryDbTests"`
Expected: 全部 passed（新增报废断言 80；原有 75 等断言不受影响，因为它们的种子不含报废单）。

- [ ] **Step 5: Commit**

```bash
git add src/ErpApi/Engines/Inventory/MaterialInventoryService.cs tests/ErpApi.Tests/MaterialInventoryDbTests.cs
git commit -m "feat(报废单): 库存引擎加报废分支(数量×-1,只认已审核)+测试"
```

---

## Task 6: 后端 Controller + DI 注册

**Files:**
- Create: `src/ErpApi/Features/Materials/MaterialScrap/MaterialScrapController.cs`
- Modify: `src/ErpApi/Program.cs:47`（退料注册行后追加报废）

镜像 `MaterialReturnController.cs`：路由 `api/material-scraps`，`Menu=Table="报废单"`、`口径="物料"`。

- [ ] **Step 1: 写 Controller**

```csharp
using System.Security.Claims;
using ErpApi.Engines.Authorization;
using ErpApi.Engines.Posting;
using ErpApi.Features.MonthEnd;
using ErpApi.Infrastructure.Db;
using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Mvc;
namespace ErpApi.Features.Materials.MaterialScrap;

[ApiController]
[Authorize]
[Route("api/material-scraps")]
public sealed class MaterialScrapController(
    MaterialScrapService svc, IPostingEngine posting, IPermissionService perms,
    IAuditLogger audit, ISqlConnectionFactory factory, PeriodLockService periodLock) : ControllerBase
{
    private const string Menu = "报废单";
    private const string Table = "报废单";
    private const string 口径 = "物料";

    private string CurrentUser =>
        User.FindFirstValue(ClaimTypes.NameIdentifier) ?? User.FindFirstValue("sub") ?? "";
    private Task<bool> AllowAsync(PermissionAction a) => perms.HasAsync(CurrentUser, Menu, a);

    private async Task AuditAsync(string behavior, string record)
    {
        using var c = factory.Create();
        await c.OpenAsync();
        await audit.WriteAsync(Table, behavior, CurrentUser, record, c);
    }

    private static void MaskDetail(MaterialScrapDetailDto d)
    {
        if (d.单头 is not null) d.单头.金额 = null;
        foreach (var l in d.明细) { l.单价 = null; l.金额 = null; }
    }

    [HttpGet]
    public async Task<IActionResult> List(int page = 1, int size = 20, string? keyword = null)
    {
        if (!await AllowAsync(PermissionAction.打开)) return Forbid();
        var result = await svc.ListAsync(page, size, keyword);
        if (!await AllowAsync(PermissionAction.单价))
            foreach (var h in result.Items) h.金额 = null;
        return Ok(result);
    }

    [HttpGet("{单号}")]
    public async Task<IActionResult> Get(string 单号)
    {
        if (!await AllowAsync(PermissionAction.打开)) return Forbid();
        var d = await svc.GetAsync(单号);
        if (d is null) return NotFound();
        if (!await AllowAsync(PermissionAction.单价)) MaskDetail(d);
        return Ok(d);
    }

    [HttpPost]
    public async Task<IActionResult> Create([FromBody] MaterialScrapCreateDto dto)
    {
        if (!await AllowAsync(PermissionAction.保存)) return Forbid();
        try { await periodLock.EnsureWarehouseOpenAsync(口径, dto.仓库, DateTime.Now); }
        catch (PeriodLockedException ex) { return Conflict(new { 消息 = ex.Message }); }
        string 单号;
        try { 单号 = await svc.CreateAsync(dto, CurrentUser); }
        catch (ArgumentException ex) { return BadRequest(new { 消息 = ex.Message }); }
        await AuditAsync("新增", $"单号={单号}");
        return CreatedAtAction(nameof(Get), new { 单号 }, new { 单号 });
    }

    [HttpDelete("{单号}")]
    public async Task<IActionResult> Delete(string 单号)
    {
        if (!await AllowAsync(PermissionAction.删除)) return Forbid();
        try { if (!await svc.DeleteAsync(单号)) return NotFound(); }
        catch (InvalidOperationException ex) { return Conflict(new { 消息 = ex.Message }); }
        await AuditAsync("删除", $"单号={单号}");
        return NoContent();
    }

    [HttpPost("{单号}/approve")]
    public async Task<IActionResult> Approve(string 单号)
    {
        if (!await AllowAsync(PermissionAction.审核)) return Forbid();
        try { await periodLock.EnsureHeaderOpenAsync(口径, Table, 单号); }
        catch (PeriodLockedException ex) { return Conflict(new { 消息 = ex.Message }); }
        if (!await posting.ApproveAsync(Table, 单号, CurrentUser))
            return Conflict(new { 消息 = "审核失败：单不存在或已审核。" });
        return NoContent();
    }

    [HttpPost("{单号}/unapprove")]
    public async Task<IActionResult> Unapprove(string 单号)
    {
        if (!await AllowAsync(PermissionAction.反审核)) return Forbid();
        try { await periodLock.EnsureHeaderOpenAsync(口径, Table, 单号); }
        catch (PeriodLockedException ex) { return Conflict(new { 消息 = ex.Message }); }
        if (!await posting.UnapproveAsync(Table, 单号, CurrentUser))
            return Conflict(new { 消息 = "反审核失败：单不存在或未审核。" });
        return NoContent();
    }
}
```

- [ ] **Step 2: DI 注册**

`src/ErpApi/Program.cs:47` 退料注册行下面加一行：
```csharp
builder.Services.AddScoped<ErpApi.Features.Materials.MaterialScrap.MaterialScrapService>();
```

- [ ] **Step 3: 编译确认**

Run: `dotnet build src/ErpApi/ErpApi.csproj`
Expected: 成功。

- [ ] **Step 4: Commit**

```bash
git add src/ErpApi/Features/Materials/MaterialScrap/MaterialScrapController.cs src/ErpApi/Program.cs
git commit -m "feat(报废单): 后端 Controller(REST+审核+成本保密)+DI注册"
```

---

## Task 7: 权限目录 + 权限种子

**Files:**
- Modify: `src/ErpApi/Features/Admin/MenuCatalog.cs:15`
- Create: `db/seed_scrap_perms.sql`

- [ ] **Step 1: MenuCatalog 加报废单**

把 `MenuCatalog.cs:15` 里 `new("物料管理","退料单"),` 后面插入 `new("物料管理","报废单"),`，即该行变为：
```csharp
        new("物料管理","采购订单"), new("物料管理","采购入仓单"), new("物料管理","采购退仓单"), new("物料管理","领料单"), new("物料管理","退料单"), new("物料管理","报废单"), new("物料管理","物料库存"),
```

- [ ] **Step 2: 写权限种子 `db/seed_scrap_perms.sql`**

```sql
-- 开发用：给某用户授予 报废单 菜单的 9 位权限。
-- 用法：把 @用户 改成你的登录名，在目标库执行。
DECLARE @用户 nvarchar(30) = N'admin';
DELETE FROM [userbqrpower] WHERE [用户]=@用户 AND [菜单] IN (N'报废单');
INSERT INTO [userbqrpower]([用户],[菜单],[打开],[保存],[删除],[打印],[单价],[金额],[审核],[反审核],[功能])
VALUES (@用户,N'报废单',1,1,1,1,1,1,1,1,1);
```

- [ ] **Step 3: 给 admin 授权（开发库 + 测试库）**

```powershell
dotnet run --project tools/DbDeploy -- ([Environment]::GetEnvironmentVariable("ERP_DB","User")) db/seed_scrap_perms.sql
dotnet run --project tools/DbDeploy -- ([Environment]::GetEnvironmentVariable("ERP_TEST_DB","User")) db/seed_scrap_perms.sql
```
Expected: 各插入 1 行（admin 拿到报废单全权限）。

- [ ] **Step 4: 编译确认**

Run: `dotnet build src/ErpApi/ErpApi.csproj`
Expected: 成功。

- [ ] **Step 5: Commit**

```bash
git add src/ErpApi/Features/Admin/MenuCatalog.cs db/seed_scrap_perms.sql
git commit -m "feat(报废单): 权限目录(物料管理/报废单)+权限种子"
```

---

## Task 8: 前端 — 配置 + 菜单

**Files:**
- Modify: `web/src/pages/materials/materialDocConfigs.ts`（在 MATERIAL_DOC_CONFIGS 里加 material-scraps）
- Modify: `web/src/nav/menuTree.tsx:76`（报废单占位补路径）

前端无新组件——`materials/:doc` 通配路由会按 config 渲染通用单据页（白嫖录入/🔍/审核/复制/打印）。

- [ ] **Step 1: 加 config 条目**

在 `materialDocConfigs.ts` 的 `MATERIAL_DOC_CONFIGS` 对象里，`"material-returns"` 条目之后（`"purchase-returns"` 之前）插入：

```ts
  "material-scraps": {
    resource: "material-scraps", menu: "报废单", title: "报废", usageCols: true,
    // 按退料单同构表头：部门/日期/报废人(🔍)/电脑单号/操作员/仓库/备注
    headerFields: [
      { name: "报废部门", label: "部门" }, { name: "日期", label: "日期", type: "date-today" },
      { name: "报废人", label: "报废人", type: "employee" }, { name: "单号", label: "电脑单号", type: "docno" },
      { name: "操作员", label: "操作员", type: "operator" }, { name: "仓库", label: "仓库", required: true },
      { name: "备注", label: "备注" },
    ],
    listExtra: [{ name: "报废部门", label: "报废部门" }, { name: "报废人", label: "报废人" }, { name: "仓库", label: "仓库" }],
  },
```

- [ ] **Step 2: 菜单补路径**

`web/src/nav/menuTree.tsx:76` 把 `M("报废单"),` 改为：
```tsx
    M("报废单", "/materials/material-scraps", "报废单"),
```

- [ ] **Step 3: 前端测试 + 构建**

Run: `npm --prefix web run test -- --run`
Expected: 现有 42 测试不回归。
Run: `npm --prefix web run build`
Expected: 构建成功（tsc 无错）。

- [ ] **Step 4: Commit**

```bash
git add web/src/pages/materials/materialDocConfigs.ts web/src/nav/menuTree.tsx
git commit -m "feat(报废单): 前端 config(material-scraps,usageCols)+菜单路径"
```

---

## Task 9: 端到端验证 + 合并

**Files:** 无（验证 + 收尾）

- [ ] **Step 1: 后端全量测试**

先停 ErpApi。Run: `dotnet test`
Expected: 全绿（含新增 MaterialScrap 4 测 + 库存报废 1 测；约 297 通过）。

- [ ] **Step 2: 起后端 + 前端**

```powershell
$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")
$env:ERP_DB = [Environment]::GetEnvironmentVariable("ERP_DB","User")
$env:ERP_JWT_KEY = [Environment]::GetEnvironmentVariable("ERP_JWT_KEY","User")
$env:ASPNETCORE_URLS = "http://localhost:5000"
Start-Process dotnet -ArgumentList "run --project src/ErpApi/ErpApi.csproj -c Release --no-launch-profile" -WindowStyle Hidden
# 前端若未起：npm --prefix web run dev
```
等 5000 起来（轮询端口）。

- [ ] **Step 2.5: 冒烟脚本验证（puppeteer，绕过系统代理）**

写 `tmp/shot/scrap-e2e.cjs`（参照 `tmp/shot/return-form.cjs`：登录 admin/admin123 → 开 `/materials/material-scraps` → 点「新建报废单」→ 点两次「加一行」→ 截图 + 抓表头/明细列文字）。运行：
```powershell
node tmp/shot/scrap-e2e.cjs
```
Expected 输出：
- 表头字段: `["部门","日期","报废人","电脑单号","操作员","仓库","备注"]`
- 明细列: `["装配采购","生产单号","款号","物料编号","物料名称","规格","材料","颜色","单位","数量","备注"]`

- [ ] **Step 3: 手工/脚本核对库存方向**

用 .NET HttpClient(UseProxy=false) 或浏览器：建一张报废单（物料仓 + 某有库存物料，数量 N）→ 审核 → 打开「物料库存」该物料，确认库存比审核前**少 N**；反审核后恢复。

- [ ] **Step 4: 合并 master（用 finishing-a-development-branch 技能）**

```bash
git checkout master
git merge --no-ff feat-scrap-doc -m "Merge branch 'feat-scrap-doc' into master"
git branch -d feat-scrap-doc
```

- [ ] **Step 5: 收尾**

更新记忆（报废单已建，库存−，复用通用组件）；重启 5000/5173（见 [[restart-servers-after-plan]]）。

---

## 自查（写完计划回看 spec）

- **Spec 覆盖**：DB 两表(T1)✓、后端三件套(T2/T3/T6)✓、库存负号(T5)✓、过账白名单(T4)✓、权限目录+种子(T7)✓、前端 config+菜单(T8)✓、测试(T3/T5/T9)✓。无遗漏。
- **占位符扫描**：无 TBD/TODO；每步含完整代码/命令/期望输出。
- **类型一致**：`MaterialScrapCreateDto`/`MaterialScrapHeaderDto`/`MaterialScrapDetailDto`/`MaterialScrapService`(DocType=报废单,Prefix=BF)/`MaterialScrapController`(Menu=Table=报废单,口径=物料) 跨任务一致；表名 报废单/报废明细单、列 报废部门/报废人 一致；前端 resource=`material-scraps`、菜单 perm=`报废单` 与后端 Menu 一致。
```
